{
  "name": "@matrixflow/worker",
  "version": "0.1.0",
  "private": true,
  "description": "MatrixFlow AI · BullMQ Worker（工作流/解析/向量化）",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:coverage": "jest --coverage --runInBand",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "bullmq": "^6.0.9",
    "ioredis": "6.0.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
